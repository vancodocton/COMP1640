@model IEnumerable<WebApp.ViewModels.UserViewModel>

@{
	ViewData["Title"] = "Index";
	int i = 0, j = 0;
}
<h1 class="text-center">List of Accounts</h1>

<div class="text-center d-flex justify-content-between mx-auto w-50">
<a class="btn btn-primary" asp-area="Admin" asp-controller="Account" asp-action="Create" id="add-button"><i class="fa fa-plus-square-o"></i> Create account</a>
<a class="btn btn-danger" asp-area="Admin" asp-controller="Account" asp-action="ResetPwd" id="reset-pwd-button">Reset Password</a>
</div>
<div>
	<div class="container-fluid d-flex justify-content-center align-items-center h-100">
		<div class="form-input p-3 text-center py-4">
			<table class="table table-striped">
				<thead>
					<tr>
						<th scope="col">ID</th>
						<th scope="col">Username</th>
						<th scope="col">Full Name</th>
						<th scope="col">Action</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var user in Model)
					{
						{ i++; j++; }
						<tr>
							<th scope="row">@i</th>
							<td>@user.UserName</td>
							<td>@user.FullName</td>
							@*                            <td>
								@string.Join(", ", user.Roles)
								</td>*@
							<td class="dropdown">
								<button class="btn btn-primary dropdown-toggle" type="button" id="actions-dropdown-@j" data-bs-toggle="dropdown"
									aria-haspopup="true" aria-expanded="false">
									Actions
								</button>
								<div class="dropdown-menu dropdown-menu-end bg-light" aria-labelledby="actions-dropdown-@j">
									<a class="dropdown-item text-success" asp-area="Admin" asp-controller="Account" asp-action="Update" asp-route-id="@user.Id">
										Edit
									</a>
									<a class="dropdown-item text-danger" asp-area="Admin" asp-controller="Account" asp-action="Delete" asp-route-id="@user.Id">
										Delete
									</a>
									<a class="dropdown-item text-danger" asp-area="Admin" asp-controller="Account" asp-action="ResetPwd" asp-route-id="@user.Email">
										Reset Password
									</a>
								</div>
							</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>